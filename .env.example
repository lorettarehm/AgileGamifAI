# AgileGamifAI Environment Variables
# Copy this file to .env and fill in your actual values

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anonymous_key

# LLM API Configuration
# SECURITY WARNING: In Vite applications, variables prefixed with VITE_ are exposed 
# in the client-side bundle. This means the API key will be visible to end users.
# For production applications, consider:
# 1. Moving LLM calls to a backend API
# 2. Using serverless functions as a proxy
# 3. Implementing user-based API key management
VITE_HF_ACCESS_TOKEN=your_huggingface_access_token