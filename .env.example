# AgileGamifAI Environment Variables
# Copy this file to .env and fill in your actual values
#
# ⚠️  CRITICAL SECURITY WARNING ⚠️
# ALL variables prefixed with VITE_ are EXPOSED in the client-side JavaScript bundle
# Anyone can extract these values from your deployed application
# NEVER use production API keys or high-limit keys in client-side applications

# Supabase Configuration (Database)
# Get these from: https://supabase.com/dashboard -> Your Project -> Settings -> API
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anonymous_key

# HuggingFace API Configuration (AI/LLM)
# Get from: https://huggingface.co/settings/tokens
# ⚠️  SECURITY: This key will be visible to ALL users of your app
# Recommendations:
# - Use a demo/test account with limited quotas
# - Set usage limits and monitoring
# - Rotate keys regularly
# - For production, implement backend API instead
VITE_HF_ACCESS_TOKEN=your_huggingface_access_token

# Development Tips:
# 1. Use .env.local (git-ignored) for personal development keys
# 2. Create separate accounts for development vs demo vs production
# 3. Set up usage alerts on your API accounts
# 4. Monitor for unexpected usage that might indicate key misuse
# 5. Consider using mock responses for public demos